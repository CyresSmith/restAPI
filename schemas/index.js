const { Contact, contactsValidation } = require('../schemas/contact');
const { User, userValidation } = require('../schemas/user');

module.exports = { Contact, contactsValidation, User, userValidation };
